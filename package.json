{
  "name": "@targoninc/ts-mail",
  "type": "module",
  "version": "0.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/targoninc/ts-mail.git"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun --format esm && tsc",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@targoninc/ts-logging": "^0.0.1",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^6.4.17",
    "bun-plugin-dtsx": "^0.21.9"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
